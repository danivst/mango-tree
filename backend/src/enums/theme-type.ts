// Re-export from shared package for backward compatibility
export { ThemeType, ThemeTypeValue } from "@mangotree/shared";

// Maintain default export for backward compatibility
export { ThemeTypeValue as default } from "@mangotree/shared";
